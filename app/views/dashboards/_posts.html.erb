<h3>Posts</h3>
<p>These are our posts</p>
<div class="post_content">
  <% @posts.each do |post| %>
  <h1 class="title"><%= post.title %></h1>
  <p class="date">
    Submitted <%= time_ago_in_words(post.created_at) %> Ago
  </p>
  <%= link_to 'Link', post_path(post) %>
  <% end %>
</div>
