<%= form_for @skill do |f| %>
<% if @skill.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

  <ul>
    <% @skill.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :skillcategory %>
  <%= f.collection_select(:skillcategory_id, SkillCategory.all, :id, :name) %>
</div>

<div class="field">
  <%= f.label :score %>
  <%= f.number_field :score %>
</div>

<div class="actions">
  <%= f.submit %>
</div>
<% end %>
